# CLAUDE.md — Agent Entry Point

> This repo's canonical agent rules live in `RULES.md` (and `AGENTS.md` Quick Reference).
> All agents (Claude Code, opencode, Cursor, etc.) MUST read `RULES.md` first on every session and follow it verbatim — it survives compaction and is normative.

- **Read first:** `RULES.md` → then `AGENTS.md` → then `docs/architecture.md`
- **Follow always:** Hard blocks, layered architecture (flat), verification & evidence, delegation protocol — see `RULES.md` §2.8
- **Compaction:** Re-read `RULES.md` + `AGENTS.md` after compaction
