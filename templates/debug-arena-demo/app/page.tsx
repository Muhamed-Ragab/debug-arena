'use client'

import { useState } from 'react'
import {
  Activity, ArrowLeft, ArrowRight, BarChart3, Bell, BookOpen, Check, ChevronDown, ChevronRight,
  CircleHelp, Clock3, Code2, Command, FileCode2, FileText, Filter, Flame, GitBranch, LayoutGrid,
  Lightbulb, ListFilter, Menu, Moon, Play, Plus, Search, Settings2, ShieldCheck, Sparkles, Sun,
  Terminal, Trophy, UserRound, X, Zap,
} from 'lucide-react'

const categories = {
  React: { color: 'text-sky-400', bg: 'bg-sky-400/10', border: 'border-sky-400/30', dot: 'bg-sky-400' },
  Backend: { color: 'text-amber-400', bg: 'bg-amber-400/10', border: 'border-amber-400/30', dot: 'bg-amber-400' },
  Distributed: { color: 'text-fuchsia-400', bg: 'bg-fuchsia-400/10', border: 'border-fuchsia-400/30', dot: 'bg-fuchsia-400' },
  Performance: { color: 'text-emerald-400', bg: 'bg-emerald-400/10', border: 'border-emerald-400/30', dot: 'bg-emerald-400' },
}
const challenges = [
  { id: 1, category: 'React', difficulty: 'Medium', time: '12 min', title: 'The dashboard freezes after the third click', desc: 'A customer reports that filtering their activity feed becomes unresponsive after a few interactions.', solved: 68, points: 350 },
  { id: 2, category: 'Backend', difficulty: 'Hard', time: '18 min', title: 'Orders occasionally disappear from the queue', desc: 'Your fulfillment team sees orders vanish between payment confirmation and processing.', solved: 41, points: 500 },
  { id: 3, category: 'Distributed', difficulty: 'Hard', time: '22 min', title: 'Users are seeing yesterday’s feature flags', desc: 'A staged rollout looks healthy, but a subset of users never receives the new configuration.', solved: 29, points: 550 },
  { id: 4, category: 'Performance', difficulty: 'Easy', time: '8 min', title: 'The first page load takes eight seconds', desc: 'Synthetic checks pass locally, but production users wait for a blank screen on mobile.', solved: 82, points: 250 },
  { id: 5, category: 'React', difficulty: 'Easy', time: '7 min', title: 'The modal closes while you are typing', desc: 'Support agents lose their draft when a background refresh completes.', solved: 76, points: 220 },
  { id: 6, category: 'Backend', difficulty: 'Medium', time: '14 min', title: 'The report total changes on refresh', desc: 'Finance cannot reconcile a report because the same date range returns different totals.', solved: 53, points: 390 },
]
const code = [
  'import { useMemo, useState } from "react"',
  'import { ActivityRow } from "./activity-row"',
  '',
  'export function ActivityFeed({ events, query }) {',
  '  const [selected, setSelected] = useState(null)',
  '',
  '  const filteredEvents = useMemo(() => {',
  '    return events.filter((event) => {',
  '      return event.title.includes(query)',
  '    })',
  '  }, [events, query])',
  '',
  '  return (',
  '    <div className="feed">',
  '      {filteredEvents.map((event) => (',
  '        <ActivityRow key={event.id}',
  '          event={event}',
  '          selected={selected === event.id}',
  '          onClick={() => setSelected(event.id)}',
  '        />',
  '      ))}',
  '    </div>',
  '  )',
  '}',
]

type Screen = 'browser' | 'challenge' | 'results' | 'profile' | 'leaderboard'

function Badge({ children, tone = 'muted' }: { children: React.ReactNode; tone?: string }) {
  const styles: Record<string, string> = { muted: 'border-border text-muted-foreground bg-muted/40', easy: 'border-emerald-400/25 text-emerald-400 bg-emerald-400/8', medium: 'border-amber-400/25 text-amber-400 bg-amber-400/8', hard: 'border-rose-400/25 text-rose-400 bg-rose-400/8' }
  return <span className={`inline-flex items-center gap-1 rounded-md border px-2 py-0.5 font-mono text-[10px] font-medium uppercase tracking-wider ${styles[tone] || tone}`}>{children}</span>
}
function Category({ name }: { name: keyof typeof categories }) { const c = categories[name]; return <span className={`inline-flex items-center gap-1.5 font-mono text-[10px] uppercase tracking-wider ${c.color}`}><i className={`size-1.5 rounded-full ${c.dot}`} />{name}</span> }

function Shell({ screen, setScreen, dark, setDark, children }: { screen: Screen; setScreen: (s: Screen) => void; dark: boolean; setDark: (v: boolean) => void; children: React.ReactNode }) {
  return <div className="min-h-screen bg-background text-foreground"><header className="flex h-14 items-center justify-between border-b border-border px-4 lg:px-6"><div className="flex items-center gap-3"><button className="rounded-md p-2 text-muted-foreground hover:bg-muted md:hidden"><Menu size={18} /></button><button onClick={() => setScreen('browser')} className="flex items-center gap-2"><span className="grid size-7 place-items-center rounded-md bg-primary text-primary-foreground"><Terminal size={15} /></span><span className="font-mono text-sm font-semibold tracking-tight">debug<span className="text-muted-foreground">/</span>arena</span></button><span className="hidden h-5 w-px bg-border md:block" /><span className="hidden font-mono text-[10px] uppercase tracking-widest text-muted-foreground md:block">engineering practice</span></div><div className="flex items-center gap-1"><button className="rounded-md p-2 text-muted-foreground hover:bg-muted" aria-label="Search"><Search size={17} /></button><button onClick={() => setDark(!dark)} className="rounded-md p-2 text-muted-foreground hover:bg-muted" aria-label="Toggle theme">{dark ? <Sun size={17} /> : <Moon size={17} />}</button><button className="ml-1 flex items-center gap-2 rounded-md border border-border px-2 py-1.5 text-xs hover:bg-muted"><span className="grid size-5 place-items-center rounded-full bg-primary/15 text-[10px] font-semibold text-primary">AR</span><span className="hidden sm:block">Alex Rivera</span><ChevronDown size={13} className="text-muted-foreground" /></button></div></header><div className="flex"><aside className="hidden min-h-[calc(100vh-3.5rem)] w-56 shrink-0 border-r border-border p-3 md:block"><div className="mb-6 px-2 pt-2"><p className="font-mono text-[9px] uppercase tracking-[0.2em] text-muted-foreground">Workspace</p></div><nav className="space-y-1">{([['browser', LayoutGrid, 'Challenges'], ['profile', BarChart3, 'Your progress'], ['leaderboard', Trophy, 'Leaderboard']] as const).map(([id, Icon, label]) => <button key={id} onClick={() => setScreen(id)} className={`flex w-full items-center gap-3 rounded-md px-3 py-2 text-sm ${screen === id ? 'bg-muted font-medium text-foreground' : 'text-muted-foreground hover:bg-muted/60 hover:text-foreground'}`}><Icon size={16} />{label}{id === 'profile' && <span className="ml-auto font-mono text-[10px] text-emerald-400">+12%</span>}</button>)}</nav><div className="my-6 border-t border-border" /><div className="px-2"><p className="mb-3 font-mono text-[9px] uppercase tracking-[0.2em] text-muted-foreground">Your focus</p><div className="space-y-3 text-xs"><div className="flex items-center gap-2"><i className="size-1.5 rounded-full bg-sky-400" /><span>React rendering</span><span className="ml-auto font-mono text-muted-foreground">8</span></div><div className="flex items-center gap-2"><i className="size-1.5 rounded-full bg-amber-400" /><span>Backend systems</span><span className="ml-auto font-mono text-muted-foreground">5</span></div><div className="flex items-center gap-2"><i className="size-1.5 rounded-full bg-fuchsia-400" /><span>Distributed</span><span className="ml-auto font-mono text-muted-foreground">3</span></div></div></div><div className="mt-auto pt-40"><button className="flex w-full items-center gap-3 rounded-md px-3 py-2 text-sm text-muted-foreground hover:bg-muted"><Settings2 size={16} />Settings</button></div></aside><main className="min-w-0 flex-1">{children}</main></div></div>
}

function Browser({ setScreen, selected, setSelected }: { setScreen: (s: Screen) => void; selected: (typeof challenges)[number] | null; setSelected: (c: (typeof challenges)[number]) => void }) {
  const [filter, setFilter] = useState('All'); const [query, setQuery] = useState('')
  const shown = challenges.filter(c => (filter === 'All' || c.category === filter) && c.title.toLowerCase().includes(query.toLowerCase()))
  return <div className="mx-auto max-w-7xl px-5 py-8 lg:px-10"><div className="flex flex-col justify-between gap-5 border-b border-border pb-7 sm:flex-row sm:items-end"><div><div className="mb-3 flex items-center gap-2 font-mono text-[10px] uppercase tracking-widest text-muted-foreground"><span className="text-emerald-400">/</span> challenge browser <span>/</span> all incidents</div><h1 className="text-3xl font-semibold tracking-tight sm:text-4xl">Find the bug.</h1><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">Practice the messy, ambiguous debugging problems that show up in production.</p></div><div className="flex items-center gap-2 font-mono text-xs text-muted-foreground"><Flame size={15} className="text-orange-400" /> 7 day streak <span className="mx-1 text-border">·</span> <span className="text-foreground">1,240 XP</span></div></div><div className="flex flex-col justify-between gap-3 py-5 sm:flex-row sm:items-center"><div className="flex flex-wrap gap-2">{['All', 'React', 'Backend', 'Distributed', 'Performance'].map(x => <button key={x} onClick={() => setFilter(x)} className={`rounded-md border px-3 py-1.5 text-xs ${filter === x ? 'border-foreground/30 bg-muted text-foreground' : 'border-transparent text-muted-foreground hover:border-border'}`}>{x}</button>)}</div><div className="relative"><Search size={14} className="absolute left-3 top-2.5 text-muted-foreground" /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search incidents" className="h-8 w-full rounded-md border border-border bg-card pl-9 pr-3 text-xs outline-none placeholder:text-muted-foreground focus:ring-1 focus:ring-ring sm:w-52" /></div></div><div className="mb-4 flex items-center justify-between"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground"><span className="text-foreground">{shown.length}</span> incidents available</p><button className="flex items-center gap-2 text-xs text-muted-foreground hover:text-foreground"><ListFilter size={14} /> Sort: Recommended <ChevronDown size={13} /></button></div><div className="grid gap-3 lg:grid-cols-2">{shown.map(c => <button key={c.id} onClick={() => { setSelected(c); setScreen('challenge') }} className="group text-left"><div className="rounded-lg border border-border bg-card p-5 transition hover:border-foreground/30 hover:bg-muted/30"><div className="flex items-center justify-between"><Category name={c.category as keyof typeof categories} /><Badge tone={c.difficulty.toLowerCase()}>{c.difficulty}</Badge></div><h2 className="mt-5 text-[15px] font-medium tracking-tight group-hover:text-primary">{c.title}</h2><p className="mt-2 min-h-10 text-sm leading-5 text-muted-foreground">{c.desc}</p><div className="mt-5 flex items-center justify-between border-t border-border pt-4 font-mono text-[10px] text-muted-foreground"><span className="flex items-center gap-1.5"><Clock3 size={12} />{c.time}</span><span>{c.solved}% solved</span><span className="text-foreground">{c.points} pts <ArrowRight size={12} className="ml-1 inline transition group-hover:translate-x-1" /></span></div></div></button>)}</div></div>
}

function Challenge({ challenge, setScreen }: { challenge: (typeof challenges)[number]; setScreen: (s: Screen) => void }) {
  const [tab, setTab] = useState('Explain'); const [line, setLine] = useState(7); const [hint, setHint] = useState(false); const [submitting, setSubmitting] = useState(false)
  return <div className="flex h-[calc(100vh-3.5rem)] flex-col"><div className="flex h-12 shrink-0 items-center justify-between border-b border-border px-4"><div className="flex items-center gap-3"><button onClick={() => setScreen('browser')} className="text-muted-foreground hover:text-foreground"><ArrowLeft size={16} /></button><span className="hidden font-mono text-[10px] uppercase tracking-widest text-muted-foreground sm:block">INC-{String(challenge.id).padStart(3, '0')}</span><Category name={challenge.category as keyof typeof categories} /><Badge tone={challenge.difficulty.toLowerCase()}>{challenge.difficulty}</Badge></div><div className="flex items-center gap-4 font-mono text-[10px] text-muted-foreground"><span className="hidden items-center gap-1 sm:flex"><Clock3 size={13} /> 11:42</span><span>0 / 4 localized</span><button onClick={() => { setSubmitting(true); setTimeout(() => setScreen('results'), 900) }} disabled={submitting} className="flex items-center gap-2 rounded-md bg-primary px-3 py-1.5 font-sans text-xs font-medium text-primary-foreground disabled:opacity-60">{submitting ? <><Activity size={13} className="animate-pulse" /> Running tests...</> : <><Play size={13} /> Submit debug</>}</button></div></div><div className="grid min-h-0 flex-1 lg:grid-cols-[260px_minmax(360px,1fr)_minmax(340px,0.9fr)]"><section className="hidden overflow-auto border-r border-border bg-card/30 p-5 lg:block"><div className="mb-5 flex items-center gap-2 font-mono text-[10px] uppercase tracking-widest text-muted-foreground"><Bell size={13} className="text-rose-400" /> incident report</div><h1 className="text-lg font-medium leading-7">{challenge.title}</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">At 02:14 UTC, the support team noticed users could no longer interact with the activity feed. The page becomes unresponsive after the third filter change.</p><div className="mt-6 rounded-md border border-rose-400/20 bg-rose-400/5 p-3"><p className="font-mono text-[9px] uppercase tracking-widest text-rose-400">Observed behavior</p><p className="mt-2 text-xs leading-5 text-muted-foreground">CPU spikes to 100%. No server errors. Reproduces only when the list has more than 50 events.</p></div><div className="mt-7"><p className="mb-3 font-mono text-[9px] uppercase tracking-widest text-muted-foreground">Files</p><div className="space-y-1 font-mono text-xs"><div className="flex items-center gap-2 rounded bg-muted px-2 py-1.5"><FileCode2 size={13} className="text-sky-400" /> activity-feed.tsx</div><div className="flex items-center gap-2 px-2 py-1.5 text-muted-foreground"><FileText size={13} /> activity-row.tsx</div><div className="flex items-center gap-2 px-2 py-1.5 text-muted-foreground"><FileText size={13} /> filters.ts</div></div></div></section><section className="min-h-0 overflow-auto border-r border-border bg-[#111315]"><div className="flex h-10 items-center gap-4 border-b border-border px-4 font-mono text-[10px] text-muted-foreground"><span className="border-b-2 border-sky-400 py-3 text-foreground">activity-feed.tsx</span><span>×</span><span className="ml-auto flex items-center gap-1"><GitBranch size={12} /> main</span></div><div className="p-4 font-mono text-xs leading-6"><p className="mb-3 text-[10px] text-muted-foreground">Click a line to localize the issue <span className="text-sky-400">·</span> selected line: {line}</p>{code.map((row, i) => <button key={i} onClick={() => setLine(i + 1)} className={`flex w-full rounded-sm text-left ${line === i + 1 ? 'bg-amber-400/15 text-amber-100' : 'hover:bg-white/5'}`}><span className="w-9 shrink-0 select-none pr-3 text-right text-[#59636e]">{i + 1}</span><span className={`${line === i + 1 ? 'border-l-2 border-amber-400 pl-2' : 'pl-2'} whitespace-pre text-[#b8c2cc]`}>{row || ' '}</span></button>)}</div></section><section className="min-h-0 overflow-auto bg-card/40"><div className="flex border-b border-border">{['Explain', 'Fix', 'Hints'].map(x => <button key={x} onClick={() => setTab(x)} className={`flex-1 border-b-2 px-4 py-3 text-xs font-medium ${tab === x ? 'border-sky-400 text-foreground' : 'border-transparent text-muted-foreground'}`}>{x}{x === 'Hints' && <span className="ml-2 rounded bg-amber-400/10 px-1.5 py-0.5 font-mono text-[9px] text-amber-400">2</span>}</button>)}</div>{tab === 'Explain' && <div className="flex h-full flex-col p-5"><div className="mb-5"><p className="font-mono text-[10px] uppercase tracking-widest text-sky-400">01 / root cause</p><h2 className="mt-2 text-lg font-medium">What is going wrong?</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">Explain the mechanism, not just the symptom. This is worth 50% of your score.</p></div><textarea className="min-h-44 flex-1 resize-none rounded-md border border-border bg-background p-4 text-sm leading-6 outline-none placeholder:text-muted-foreground focus:border-sky-400/50" placeholder="The bug occurs because..." /><div className="mt-4 flex items-center gap-2 text-[10px] text-muted-foreground"><CircleHelp size={13} /> Be specific about the lifecycle or state transition involved.</div></div>}{tab === 'Fix' && <div className="p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-emerald-400">02 / proposed fix</p><h2 className="mt-2 text-lg font-medium">Show us the patch</h2><div className="mt-5 rounded-md border border-border bg-[#111315] p-4 font-mono text-xs leading-6"><p className="text-rose-400">{'- }, [events, query])'}</p><p className="text-emerald-400">{'+ }, [events, query, selected])'}</p><p className="mt-3 text-muted-foreground">// Explain why this prevents the freeze</p></div><textarea className="mt-4 min-h-32 w-full resize-none rounded-md border border-border bg-background p-3 font-mono text-xs outline-none placeholder:text-muted-foreground" placeholder="Optional: describe your fix..." /></div>}{tab === 'Hints' && <div className="p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-amber-400">03 / guided investigation</p><h2 className="mt-2 text-lg font-medium">Need a nudge?</h2><p className="mt-1 text-xs text-muted-foreground">Hints reduce your final score. The cost is always shown up front.</p><div className="mt-5 rounded-md border border-border p-4"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><Lightbulb size={16} className="text-amber-400" /><div><p className="text-sm font-medium">Check the memo dependencies</p><p className="mt-1 text-xs text-muted-foreground">There is a value used inside the callback that is not listed.</p></div></div><button onClick={() => setHint(true)} className="rounded-md border border-amber-400/30 px-2.5 py-1.5 font-mono text-[10px] text-amber-400 hover:bg-amber-400/10">{hint ? 'Revealed' : 'Reveal −10 pts'}</button></div>{hint && <div className="mt-4 border-t border-border pt-3 text-xs leading-5 text-muted-foreground">Look closely at the selected state and ask when the memoized function is recreated.</div>}</div></div>}</section></div></div>
}

function Results({ setScreen }: { setScreen: (s: Screen) => void }) { const [open, setOpen] = useState(true); return <div className="mx-auto max-w-6xl px-5 py-8 lg:px-10"><div className="flex items-start justify-between border-b border-border pb-7"><div><div className="mb-3 font-mono text-[10px] uppercase tracking-widest text-muted-foreground"><span className="text-emerald-400">/</span> run complete · inc-001</div><h1 className="text-3xl font-semibold tracking-tight">Good catch. Let&apos;s debrief.</h1><p className="mt-2 text-sm text-muted-foreground">You found the mechanism behind the freeze. Here&apos;s how your reasoning held up.</p></div><div className="text-right"><p className="font-mono text-4xl font-semibold text-emerald-400">82<span className="text-lg text-muted-foreground"> / 100</span></p><p className="mt-1 font-mono text-[10px] uppercase tracking-widest text-muted-foreground">above average</p></div></div><div className="grid gap-3 py-6 sm:grid-cols-4">{[['Localization','18 / 20','text-sky-400'],['Root cause','42 / 50','text-fuchsia-400'],['Fix quality','16 / 20','text-emerald-400'],['Prevention','6 / 10','text-amber-400']].map(([a,b,c]) => <div className="rounded-lg border border-border bg-card p-4" key={a}><p className="text-xs text-muted-foreground">{a}</p><p className={`mt-2 font-mono text-xl font-semibold ${c}`}>{b}</p></div>)}</div><div className="rounded-lg border border-border bg-card"><div className="border-b border-border p-5"><div className="flex items-center gap-2"><Sparkles size={15} className="text-fuchsia-400" /><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">teaching moment</p></div><h2 className="mt-2 text-lg font-medium">Compare your reasoning</h2></div><div className="grid divide-y border-b border-border md:grid-cols-2 md:divide-x md:divide-y-0"><div className="p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Your explanation</p><p className="mt-4 text-sm leading-6 text-muted-foreground">The feed freezes because the selected item changes inside the row, causing the memoized filtered list to update and rerender repeatedly.</p></div><div className="p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-fuchsia-400">Canonical root cause</p><p className="mt-4 text-sm leading-6 text-foreground">The callback closes over <code className="rounded bg-muted px-1 font-mono text-xs">selected</code>, but that value is missing from the dependency array. Stale state triggers repeated reconciliation under load.</p></div></div><div className="flex items-start gap-3 bg-fuchsia-400/5 p-5"><ArrowRight size={16} className="mt-0.5 shrink-0 text-fuchsia-400" /><p className="text-xs leading-5 text-muted-foreground"><span className="font-medium text-foreground">Grader feedback:</span> You identified the render loop and connected it to user interaction. Next time, name the stale closure explicitly — that distinction matters when choosing a fix.</p></div></div><div className="mt-5 rounded-lg border border-border bg-card"><button onClick={() => setOpen(!open)} className="flex w-full items-center justify-between p-5 text-left"><div className="flex items-center gap-3"><ShieldCheck size={17} className="text-emerald-400" /><div><p className="font-mono text-[10px] uppercase tracking-widest text-emerald-400">prevention</p><p className="mt-1 text-sm font-medium">What would prevent this bug class?</p></div></div>{open ? <ChevronDown size={17} /> : <ChevronRight size={17} />}</button>{open && <div className="border-t border-border px-5 pb-5 pt-4 text-sm leading-6 text-muted-foreground">Enable the exhaustive-deps rule in your lint configuration and treat warnings as build failures. For complex callbacks, add a focused test that rerenders with changed selection state and asserts stable interaction.</div>}</div><button onClick={() => setScreen('browser')} className="mt-6 flex items-center gap-2 text-xs text-muted-foreground hover:text-foreground"><ArrowLeft size={14} /> Back to challenge browser</button></div> }

function Profile() { return <div className="mx-auto max-w-6xl px-5 py-8 lg:px-10"><div className="flex flex-col justify-between gap-5 border-b border-border pb-7 sm:flex-row sm:items-end"><div><p className="mb-3 font-mono text-[10px] uppercase tracking-widest text-muted-foreground">/ profile / alex-rivera</p><h1 className="text-3xl font-semibold tracking-tight">Your progress</h1><p className="mt-2 text-sm text-muted-foreground">A map of the bugs you know — and the ones worth revisiting.</p></div><div className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-full bg-sky-400/10 font-semibold text-sky-400">AR</div><div><p className="text-sm font-medium">Alex Rivera</p><p className="font-mono text-[10px] text-muted-foreground">rank #184 · 1,240 XP</p></div></div></div><div className="grid gap-3 py-6 sm:grid-cols-3"><div className="rounded-lg border border-border bg-card p-5"><div className="flex items-center gap-2 text-muted-foreground"><Flame size={15} className="text-orange-400" /><span className="text-xs">Current streak</span></div><p className="mt-3 font-mono text-3xl font-semibold">7 <span className="text-sm text-muted-foreground">days</span></p><p className="mt-2 text-xs text-emerald-400">Personal best: 14 days</p></div><div className="rounded-lg border border-border bg-card p-5"><div className="flex items-center gap-2 text-muted-foreground"><Check size={15} className="text-emerald-400" /><span className="text-xs">Challenges solved</span></div><p className="mt-3 font-mono text-3xl font-semibold">24</p><p className="mt-2 text-xs text-muted-foreground">4 this week</p></div><div className="rounded-lg border border-border bg-card p-5"><div className="flex items-center gap-2 text-muted-foreground"><Trophy size={15} className="text-amber-400" /><span className="text-xs">Average score</span></div><p className="mt-3 font-mono text-3xl font-semibold">78<span className="text-sm text-muted-foreground">/100</span></p><p className="mt-2 text-xs text-emerald-400">+12% from last month</p></div></div><div className="grid gap-4 lg:grid-cols-[1.3fr_0.7fr]"><div className="rounded-lg border border-border bg-card p-5"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">skill map</p><h2 className="mt-2 text-lg font-medium">Category performance</h2></div><button className="font-mono text-[10px] text-muted-foreground">last 30 days <ChevronDown size={12} className="ml-1 inline" /></button></div><div className="mt-7 space-y-5">{[['React rendering',82,'sky'],['Backend systems',68,'amber'],['Performance',61,'emerald'],['Distributed systems',43,'fuchsia']].map(([name,value,color]) => <div key={name}><div className="mb-2 flex justify-between text-xs"><span>{name}</span><span className="font-mono text-muted-foreground">{value}%</span></div><div className="h-2 rounded-full bg-muted"><div className={`h-full rounded-full bg-${color}-400`} style={{ width: `${value}%` }} /></div></div>)}</div></div><div className="rounded-lg border border-border bg-card p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">activity</p><h2 className="mt-2 text-lg font-medium">Streak tracker</h2><div className="mt-6 grid grid-cols-7 gap-2">{Array.from({ length: 35 }, (_, i) => <i key={i} className={`aspect-square rounded-sm ${[4,5,6,10,11,12,13,17,18,24,25,26,31,32].includes(i) ? 'bg-emerald-400' : [3,9,16,23,30].includes(i) ? 'bg-emerald-400/30' : 'bg-muted'}`} />)}</div><div className="mt-5 flex justify-between font-mono text-[9px] text-muted-foreground"><span>5 weeks ago</span><span>today</span></div></div></div></div> }

function Leaderboard() { const [time, setTime] = useState('This week'); const [cat, setCat] = useState('Overall'); const people = [['Maya Chen','2,840','98%'],['Jon Bell','2,510','94%'],['Priya Shah','2,180','91%'],['Alex Rivera','1,240','78%'],['Sam Osei','1,110','76%']]; return <div className="mx-auto max-w-5xl px-5 py-8 lg:px-10"><div className="border-b border-border pb-7"><p className="mb-3 font-mono text-[10px] uppercase tracking-widest text-muted-foreground"><span className="text-amber-400">/</span> community / leaderboard</p><h1 className="text-3xl font-semibold tracking-tight">See how you stack up.</h1><p className="mt-2 text-sm text-muted-foreground">Quietly competitive. Loudly better at debugging.</p></div><div className="flex flex-col justify-between gap-4 py-5 sm:flex-row"><div className="flex rounded-md border border-border p-1">{['This week','All-time'].map(x => <button key={x} onClick={() => setTime(x)} className={`rounded px-3 py-1.5 text-xs ${time === x ? 'bg-muted font-medium' : 'text-muted-foreground'}`}>{x}</button>)}</div><div className="flex flex-wrap gap-2">{['Overall','React','Backend','Distributed'].map(x => <button key={x} onClick={() => setCat(x)} className={`rounded-md border px-3 py-1.5 text-xs ${cat === x ? 'border-foreground/30 bg-muted' : 'border-transparent text-muted-foreground'}`}>{x}</button>)}</div></div><div className="overflow-hidden rounded-lg border border-border bg-card"><div className="grid grid-cols-[48px_1fr_100px_90px] border-b border-border px-5 py-3 font-mono text-[10px] uppercase tracking-widest text-muted-foreground"><span>#</span><span>Debugger</span><span>Score</span><span>Avg.</span></div>{people.map((p,i) => <div key={p[0]} className={`grid grid-cols-[48px_1fr_100px_90px] items-center border-b border-border px-5 py-4 last:border-0 ${p[0] === 'Alex Rivera' ? 'bg-sky-400/5' : ''}`}><span className={`font-mono text-sm ${i < 3 ? 'text-amber-400' : 'text-muted-foreground'}`}>{String(i+1).padStart(2,'0')}</span><span className="flex items-center gap-3 text-sm"><span className={`grid size-7 place-items-center rounded-full ${i === 0 ? 'bg-amber-400/15 text-amber-400' : 'bg-muted text-muted-foreground'} text-[10px] font-medium`}>{p[0].split(' ').map(x => x[0]).join('')}</span>{p[0]}{p[0] === 'Alex Rivera' && <Badge>you</Badge>}</span><span className="font-mono text-sm">{p[1]}</span><span className="font-mono text-xs text-muted-foreground">{p[2]}</span></div>)}</div></div> }

export default function Home() { const [screen, setScreen] = useState<Screen>('browser'); const [dark, setDark] = useState(true); const [selected, setSelected] = useState<(typeof challenges)[number] | null>(challenges[0]); return <div className={dark ? 'dark' : 'light'}><Shell screen={screen} setScreen={setScreen} dark={dark} setDark={setDark}>{screen === 'browser' && <Browser setScreen={setScreen} selected={selected} setSelected={setSelected} />}{screen === 'challenge' && selected && <Challenge challenge={selected} setScreen={setScreen} />}{screen === 'results' && <Results setScreen={setScreen} />}{screen === 'profile' && <Profile />}{screen === 'leaderboard' && <Leaderboard />}</Shell></div> }
